$TITLE
$GEOMETRY

*ACES2(CHARGE=$CHARGE
       MULT=$MULTIPLICITY
       $JOBTYPE
       DROPMO=$CORE
       CALC=$WAVEFUNCTION
       REFERENCE=$REFERENCE
       UNITS=$PRINTUNITS
       COORD=$COORDTYPE
       DBOC=ON
       CC_PROG=ECC 
       MEMORY=$MEMORY
       BASIS=$BASIS)
       
